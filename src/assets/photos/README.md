Drop your photos here — they appear automatically in the Gallery and Hero sections.

- `hero.jpg` → hero portrait (replaces the placeholder frame)
- any other `*.jpg/jpeg/png/webp/avif` → gallery tiles (shown in the order they appear here)

Photos are processed via Vite's import.meta.glob, so no code changes are needed.
